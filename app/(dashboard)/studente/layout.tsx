import StudentDashboardLayout from '@/components/studente/DashboardLayout'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <StudentDashboardLayout>{children}</StudentDashboardLayout>
}
